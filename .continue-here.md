---
phase: parameter_spec_vertical_measurement
task: handoff
total_tasks: 1
status: paused
last_updated: 2026-04-29T13:46:37.586Z
---

<current_state>
KnowFabric is on `master`, currently ahead of `origin/master` by 7 commits. The tracked working tree was clean before this handoff was written. The latest completed work is a real-manual `parameter_spec` vertical run using the Trane CVGF 400-1000 chiller manual and DeepSeek `deepseek-v4-flash`.

Primary report:
`output/parameter_spec_vertical/20260428T151542Z_trane_cvgf_parameter_spec/REPORT.md`
</current_state>

<completed_work>

- Resolved docs numbering collision:
  - `docs/24_ai-assisted-compilation-pilot-milestone.md` was renamed to `docs/26_ai-assisted-compilation-pilot-milestone.md`.
  - Commit: `691b186`.
- Completed Task 8 sw_base_model ontology cutover:
  - Added `SwBaseModelOntologyClient`.
  - Removed production dependency on `OntologyClassV2`.
  - Added migration 007 and removed the model class.
  - Commit: `2b49729`.
- Prepared the parameter_spec vertical run:
  - Added backend request option support.
  - Enabled `parameter_spec` for the LLM compiler.
  - Fixed PDF parser NUL stripping.
  - Fixed timezone-aware processing timestamps.
  - Commits: `1aa3883`, `a7dbced`, `2c8c576`, `e49da66`.
- Ran one real digital chiller manual through the existing ingestion pipeline:
  - PDF: `/Volumes/TSD302/pan/a00238/05、特灵中央空调/特灵/【特灵】CVGF 400-1000离心机操作维护手册（64页）-制冷百家网.pdf`
  - Staged symlink: `tmp/parameter_spec_vertical_input/trane_cvgf_400_1000_chiller_manual.pdf`
  - Imported doc id: `doc_883beab5e0004a2c`
  - Parsed pages: 64
  - Generated chunks: 84
  - Equipment class was manually confirmed as `centrifugal_chiller` because automatic matching returned only 0.554 confidence.
- Ran the `parameter_spec` LLM vertical:
  - Run ID: `20260428T151542Z_trane_cvgf_parameter_spec`
  - LLM backend: `deepseek-parameter-spec / deepseek-v4-flash`
  - LLM calls: 8
  - Prompt tokens: 15082
  - Completion tokens: 5687
  - Estimated cost: `¥0.027038`
  - Raw LLM candidates: 12
  - Verified candidates: 9
  - Rejected candidates: 3
  - Verbatim verification rate: 75.0%
  - Rule baseline candidates: 0
  - LLM-only candidates: 9
  - Commit: `863f85f`.
- Verification passed:
  - `venv/bin/python -m pytest tests/ -x`: 153 passed.
  - `bash scripts/check-all`: all 4 gates passed.
  - Output artifacts were checked for obvious API key leakage before commit.
</completed_work>

<remaining_work>

- Human operator should review `REPORT.md`, especially the L3+ sample candidates.
- Decide next direction based on the three numbers:
  - Verbatim verification rate is 75.0%, above the suggested 70% target.
  - LLM-only finds count is 9, below the earlier informal target of 30.
  - Cost per page is approximately `¥0.000422`, far below the `¥0.5/page` target.
- If continuing this line, likely next work is prompt/context tuning to increase useful LLM-only finds before expanding to `fault_code`.
- Push `master` when ready; it is ahead of `origin/master` by 7 commits.
</remaining_work>

<decisions_made>

- Used `deepseek-v4-flash` as the single backend for the vertical run because the user confirmed it was cheap enough and wanted the real task started.
- Used `centrifugal_chiller` as the equipment class after operator confirmation because automatic matching was below the required 0.7 threshold and had an ambiguous `chiller` alternative.
- Kept `scripts/llm_backends.json` ignored and uncommitted because it contains the user's API key.
- Did not modify `docs/24_knowfabric-sw-base-model-contract.md`, the implementation plan docs, or `../sw_base_model` during the vertical run.
</decisions_made>

<blockers>

- No active technical blocker.
- The next quality gate is human-in-the-loop accuracy review of the generated `parameter_spec` candidates.
</blockers>

<human_actions_pending>

- Review `output/parameter_spec_vertical/20260428T151542Z_trane_cvgf_parameter_spec/REPORT.md`.
- Spot-check the 9 verified LLM candidates, especially the 7 L3 candidates.
- Decide whether to:
  - expand to `fault_code`,
  - improve prompt/context extraction to raise LLM-only yield,
  - test another model,
  - or open OCR work for scanned manuals.
- Push `master` if these 7 local commits should go upstream now.
</human_actions_pending>

<context>
This was a measurement run, not a feature-build. A few small pipeline fixes were needed to ingest the real Chinese PDF safely: NUL text stripping and timezone-aware timestamps. Those fixes were committed before the report commit. The report is the main artifact for operator review.

Ignored local files still include `.env`, `scripts/llm_backends.json`, `venv/`, caches, `storage/`, and `tmp/`. Do not print or commit `scripts/llm_backends.json`.

Placeholder scan found old placeholder/TBD text in existing docs:
- `docs/12_domain-package-v2-migration-checklist.md`
- `docs/05_phase-plan.md`
These were pre-existing documentation items and were not part of this run.
</context>

<next_action>
Start with: open `output/parameter_spec_vertical/20260428T151542Z_trane_cvgf_parameter_spec/REPORT.md`, review the LLM-only samples and `candidates_llm_verified.jsonl`, then ask the operator whether to tune the parameter_spec prompt/context or move to the next KO type.
</next_action>
